=== Title ===
Querying sub-documents and sub-sub-documents in <b>Mongoose</b>
=== URL ===
https://coderwall.com/p/6v5rcw/querying-sub-documents-and-sub-sub-documents-in-mongoose

r \ nThis will create two users
create two users
will create two users
r \ nRemember it is an array so you can do this to query them
is an array so you can do this to query them
n \ u003cp \ u003eRemember it is an array so you can do this to query them
is an array so you can do this to query them
Tasks before filtering: 127Tasks after filtering: 7